let operacao = (op1,op2) =>{
    let soma = op1 + op2
    let sub = op1 - op2
    let mult = op1 * op2
    let div = op1 / op2
    console.log(`Soma = ${soma} Sub = ${sub} Mult = ${mult} Div = ${div}`)
}

operacao(1,3)